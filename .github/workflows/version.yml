name: Version

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'New version to tag (like v0.0.0)'
        required: true

env:
  channel: C01DC7NL1QU

jobs:
  update-package-json-and-tag:
    name: Update package.json and tag
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
        with:
          ref: main

      - name: Set Node up
        uses: actions/setup-node@v2-beta
        with:
          node-version: '12'

      - name: Update package.json
        run: |
          git config user.name github-actions
          git config user.email github-actions@github.com
          yarn config set version-git-message "%s"
          yarn config set version-tag-prefix ""
          yarn version --new-version ${{ github.event.inputs.version }}
          git push

      - name: Push version tag
        run: git push origin refs/tags/${{ github.event.inputs.version }}

      - name: Get commit hash
        id: commit-hash
        run: echo "::set-output name=this::`git rev-parse HEAD`"

      - name: Post to Slack
        uses: actions-ecosystem/action-slack-notifier@v1
        with:
          slack_token: ${{ secrets.SLACK_TOKEN }}
          channel: ${{ env.channel }}
          message: ':up: <https://github.com/${{ github.repository }}|${{ github.repository }}> の package.json と tag を ${{ github.event.inputs.version }} に更新しました。 <https://github.com/${{ github.repository }}/commit/${{ steps.commit-hash.outputs.this }}|(commit)>'
